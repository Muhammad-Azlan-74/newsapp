# iOS Setup Checklist

This checklist confirms that all necessary iOS configurations are in place for building the Side Line app.

## ✅ Files Created/Configured

### Core Configuration Files
- [x] **Podfile** - CocoaPods dependency manager configuration
- [x] **Info.plist** - App configuration with all required permissions
- [x] **AppDelegate.swift** - App entry point and Flutter initialization
- [x] **Runner-Bridging-Header.h** - Swift-Objective-C bridge
- [x] **project.pbxproj** - Xcode project configuration

### Permission Configurations in Info.plist
- [x] **NSCameraUsageDescription** - Camera access for image_picker
- [x] **NSPhotoLibraryUsageDescription** - Photo library read access
- [x] **NSPhotoLibraryAddUsageDescription** - Photo library write access
- [x] **CFBundleURLTypes** - Deep linking configuration (newsapp://)
- [x] **UISupportedInterfaceOrientations** - Portrait mode only
- [x] **Internet access** - Enabled for API calls and webview

### App Identity
- [x] **App Name**: "Side Line" (CFBundleDisplayName and CFBundleName)
- [x] **Bundle Identifier**: com.example.newsapp (needs update for production)
- [x] **Version**: 1.0.0+1
- [x] **Minimum iOS**: 12.0

### Assets
- [x] **App Icons** - All required sizes generated in AppIcon.appiconset
  - 20x20 (@1x, @2x, @3x)
  - 29x29 (@1x, @2x, @3x)
  - 40x40 (@1x, @2x, @3x)
  - 60x60 (@2x, @3x)
  - 76x76 (@1x, @2x)
  - 83.5x83.5 (@2x)
  - 1024x1024 (@1x) - App Store

### Build Configuration
- [x] **Deployment Target**: iOS 12.0
- [x] **Swift Version**: 5.0
- [x] **Bitcode**: Disabled (NO)
- [x] **User Script Sandboxing**: Disabled (required for Flutter)
- [x] **Debug.xcconfig** - Links to Generated.xcconfig
- [x] **Release.xcconfig** - Links to Generated.xcconfig

### Flutter Plugin Dependencies (Auto-configured via Podfile)
- [x] dio - HTTP networking
- [x] app_links - Deep linking
- [x] shared_preferences - Local storage
- [x] path_provider - File system access
- [x] webview_flutter - WebView integration
- [x] image_picker - Camera and photo library
- [x] cached_network_image - Image caching
- [x] intl_phone_field - Phone number input

## ⚠️ Required Before Building

### 1. Bundle Identifier (Production Only)
**Current**: `com.example.newsapp`
**Action Required**: Change to your unique identifier before App Store submission

**How to change:**
1. Open `ios/Runner.xcworkspace` in Xcode
2. Select Runner project → Runner target
3. General tab → Bundle Identifier
4. Update to your identifier (e.g., `com.yourcompany.sideline`)

### 2. Code Signing
**Action Required**: Configure code signing in Xcode

**Steps:**
1. Open `ios/Runner.xcworkspace`
2. Select Runner project → Runner target
3. Signing & Capabilities tab
4. Select your Team from dropdown
5. Enable "Automatically manage signing"

### 3. CocoaPods Installation
**Action Required**: Run pod install (first time only)

```bash
cd ios
pod install
cd ..
```

This will:
- Install all iOS plugin dependencies
- Generate `Podfile.lock`
- Create `Pods/` directory
- Update workspace configuration

## 📋 Pre-Build Verification

Run these commands to verify setup:

```bash
# Check Flutter environment
flutter doctor -v

# Verify dependencies
flutter pub get

# Verify iOS dependencies
cd ios && pod install && cd ..

# Check for issues
flutter analyze
```

## 🚀 Build Commands

### Simulator
```bash
# Run in debug mode
flutter run

# Run in release mode
flutter run --release
```

### Physical Device
```bash
# Connect device via USB
# Select device in Xcode or use flutter devices
flutter run -d <device-id>
```

### Production Build
```bash
# Build release IPA
flutter build ios --release

# Or use Xcode:
# Product → Archive → Distribute
```

## 🔍 Troubleshooting Checklist

If build fails, check:

- [ ] Opened `Runner.xcworkspace` (not Runner.xcodeproj)
- [ ] Ran `flutter pub get`
- [ ] Ran `cd ios && pod install`
- [ ] Selected valid development team in Xcode
- [ ] Bundle identifier is unique
- [ ] Device/simulator is selected
- [ ] macOS has Xcode Command Line Tools installed
- [ ] Flutter and Xcode are up to date

## 📱 Supported Features

### Device Capabilities
- [x] Portrait orientation only (locked)
- [x] iPhone and iPad support
- [x] iOS 12.0+ compatibility
- [x] Universal app (supports all screen sizes)

### Permissions
- [x] Camera access
- [x] Photo library access
- [x] Internet connectivity
- [x] Local storage
- [x] Deep linking

### Integrations
- [x] HTTP networking (dio)
- [x] WebView (webview_flutter)
- [x] Image picker with camera
- [x] Cached network images
- [x] Deep link handling (newsapp://)
- [x] International phone numbers

## 📂 Important Files

```
ios/
├── Podfile                           ✅ Created
├── Podfile.lock                      ⏳ Generated by pod install
├── Pods/                             ⏳ Generated by pod install
├── Runner.xcworkspace/               ✅ Exists (open this!)
├── Runner.xcodeproj/                 ✅ Exists
├── Runner/
│   ├── AppDelegate.swift             ✅ Configured
│   ├── Info.plist                    ✅ Updated with permissions
│   ├── Runner-Bridging-Header.h      ✅ Configured
│   └── Assets.xcassets/
│       └── AppIcon.appiconset/       ✅ All icons generated
├── Flutter/
│   ├── Debug.xcconfig                ✅ Configured
│   ├── Release.xcconfig              ✅ Configured
│   └── Generated.xcconfig            ✅ Auto-generated
└── README.md                         ✅ Created

✅ = Ready
⏳ = Will be generated
```

## 🎯 Next Steps

1. **Transfer to Mac** - Copy project to macOS machine
2. **Open in Xcode** - `cd ios && open Runner.xcworkspace`
3. **Install Dependencies** - `cd ios && pod install`
4. **Configure Signing** - Select team in Xcode
5. **Build & Run** - Cmd+R in Xcode or `flutter run`

## ✨ Status: Ready for iOS Development

All necessary configurations are in place. The project is ready to be:
- Cloned on a Mac
- Opened in Xcode
- Built for iOS devices and simulators

**Last Updated**: 2026-01-14
**iOS Deployment Target**: 12.0+
**App Version**: 1.0.0 (Build 1)
